# Dataset

This directory is intentionally kept empty in the repository.

The project uses the **PHEME dataset**. The training script downloads the
dataset through KaggleHub when it is not already available locally.

Do not commit the raw PHEME dataset or generated graph files to GitHub.
